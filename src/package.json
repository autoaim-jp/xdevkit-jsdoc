{
	"scripts": {
		"generate": "rm -rf ./build/* && yarn jsdoc -c setting/jsdoc.config.json",
		"generate-publish": "yarn run generate && gh-pages -d build -r 'git@github.com:/autoaim-jp/xdevkit-jsdoc' -m \"update: --skip-ci\""
	},
	"type": "module",
	"devDependencies": {
		"jsdoc": "^4.0.0",
		"gh-pages": "^6.0.0"
	}
}
